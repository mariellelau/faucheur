{% extends '../base.html.twig' %}

{% block body %}
    <h1>Ajout d'une aptitude</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('aptitude_index') }}">Liste des aptitudes</a>
        </li>
    </ul>
{% endblock %}
