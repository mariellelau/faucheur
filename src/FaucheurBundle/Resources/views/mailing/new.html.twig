{% extends '@Faucheur/base.html.twig' %}

{% block body %}
    <h1>Ajout d'une mailing</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('mailing_index') }}">Liste des mailings</a>
        </li>
    </ul>
{% endblock %}
