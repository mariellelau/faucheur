{% extends '@Faucheur/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="offset-s2 col s8">
            <h1>Ajouter un militant</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Valider" />
            {{ form_end(form) }}

            <a href="{{ path('militant_index') }}">Liste des militants</a>
        </div>
    </div>

{% endblock %}
